package problem1;

/**
 * Boat is a subclass of Vehicle
 */
public class Boat extends Vehicle{
    public Boat(String ID, float avgSpeed, float maxSpeed) {
        super(ID, avgSpeed, maxSpeed);
    }
}
