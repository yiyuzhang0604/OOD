package problem1;

/**
 * Train is a subclass of Vehicle
 */
public class Train extends Vehicle{
    public Train(String ID, float avgSpeed, float maxSpeed) {
        super(ID, avgSpeed, maxSpeed);
    }
}
